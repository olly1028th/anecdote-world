import type { ExpenseCategory } from '../types/trip';

/** 경비 카테고리 목록 (TripFormPage, TripDetailPage 공통) */
export const EXPENSE_CATEGORIES: ExpenseCategory[] = [
  'flight', 'hotel', 'food', 'transport', 'activity', 'shopping', 'other',
];
