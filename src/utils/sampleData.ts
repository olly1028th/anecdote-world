import type { Trip } from '../types/trip';
import type { Pin } from '../types/database';

/** 샘플 여행 데이터 (제거됨 — 사용자 등록 데이터만 사용) */
export const sampleTrips: Trip[] = [];

/** 샘플 핀 데이터 (제거됨 — 사용자 등록 데이터만 사용) */
export const samplePins: Pin[] = [];
